═══════════════════════════════════════════════════════════════
  TypingWar.uz — BOSQICH 5 (AI + GHOST) HISOBOTI
  Sana: 2026-06-16
═══════════════════════════════════════════════════════════════

MAQSAD
------
Adaptiv AI raqib, AI yozish simulyatori, Ghost rejimi, Blind Duel,
RaceHub. Yakka o'yinchiga doimo mavjud raqiblar.

BAJARILGAN ISHLAR
-----------------
1. Domain
   - AiTypingSimulator (sof): belgi-belgi yozish jadvali — 12000/wpm ms,
     ±15ms jitter, ±3 WPM og'ish; kumulyativ ms qaytaradi. EstimatedFinishMs.

2. Application
   - IAiOpponentService interfeysi.
   - RecordResultCommand — natija saqlash mantig'i (RaceResult + PB +
     leaderboard) UserId aniq beriladi (hub lardan chaqirish uchun).
   - SubmitResultCommand → endi RecordResultCommand ga delegat qiladi
     (ICurrentUserService dan UserId oladi). Dublikat yo'q.
   - GetPersonalBestQuery — Ghost uchun PB WPM (anonim → 0).

3. Infrastructure
   - AiOpponentService: so'nggi 10 RaceResults avg WPM + delta
     (Redis ai_delta_{userId}). UpdateAfterRace: user yutsa +2 (max +10),
     yutqazsa -2 (min -10). Target = max(15, avg+delta), tarix yo'q → 40.

4. Web (SignalR + API)
   - RaceHub (/hubs/race):
       StartAiRace() → { text, textId, targetWpm, schedule } (server jadval quradi).
       FinishAiRace(timeMode, correct, incorrect, elapsed, textId, won) →
         RecordResultCommand (natija) + AiOpponentService.UpdateAfterRace (delta).
   - PracticeController: GET /api/practice/personalbest (Ghost uchun).
   - Program.cs: MapHub<RaceHub>("/hubs/race").

5. Frontend
   - /Race: rejim tanlash (🤖 AI / 👻 Ghost / 🙈 Blind Duel), "Boshlash",
     3-2-1 countdown, ikki progress bar (siz / raqib), natija (g'alaba/mag'lubiyat).
   - race.js:
       AI / Blind — RaceHub.StartAiRace dan matn+jadval, klient raqibni
         requestAnimationFrame bilan animatsiya qiladi, tugagach FinishAiRace.
       Ghost — /api/practice/personalbest dan PB, jadval KLIENTDA quriladi
         (server hisoblamaydi — spec), PB tezligida raqib.
       Blind — harflar rangsiz (xato/to'g'ri ko'rsatilmaydi).
   - Navbar "Poyga" linki. CSS: vs panel, raqib bar, blind.

TEKSHIRUV
---------
- dotnet build: 0 Warning, 0 Error.
- dotnet test: 16/16 (TypingCalculator 9, RoomCode 2, AiTypingSimulator 5).
- Ishlaydigan ilova (DLL, port 5214):
    /Race → 200.
    POST /api/practice/result (refaktor qilingan RecordResult yo'li) → 200,
      36 wpm, isNewPersonalBest=true (refaktor buzilmadi).
    GET /api/practice/personalbest?timeMode=Thirty → {"bestWpm":36}.
    GET /api/leaderboard → ishlayapti.
    POST /hubs/race/negotiate → 200, connectionId + WebSockets.
    Cheat (3000 belgi/10s) → 400 (RecordResult aldash himoyasi saqlandi).
    Loglarda xato yo'q.

QARORLAR / IZOHLAR
------------------
1. AI yozish jadvali SERVERDA (AiTypingSimulator), klient animatsiya qiladi —
   bu serverda har belgi uchun timer ushlamaslikni (yengillik) ta'minlaydi,
   lekin jadval server tomondan ishonchli quriladi.
2. Ghost SPEC bo'yicha to'liq KLIENTDA (server hisoblamaydi) — jadval JS da
   PB tezligida quriladi. Shuning uchun timing formulasi JS da takrorlangan.
3. Natija saqlash refaktori (RecordResultCommand): hub larda ICurrentUserService
   (HttpContext) null bo'lgani uchun UserId Context.User dan olinib, aniq
   uzatiladi. SubmitResultCommand API uchun yupqa o'ram bo'lib qoldi.
4. AI/Blind natijalari RaceResult sifatida saqlanadi → AI avg (so'nggi 10)
   ni oziqlantiradi va PB/leaderboard ga ham tushadi (halol natija).
5. "won" = foydalanuvchi raqibdan oldin matnni tugatsa (elapsed ≤ aiFinish).

HAL QILINMAGAN / KEYINGI BOSQICHGA
----------------------------------
- 1vs1 (ikki inson) matchmaking hali yo'q (RaceHub kelajakda kengaytiriladi).
- AI delta / StartAiRace real-time oqimi brauzerda qo'lda sinalishi kerak
  (negotiate + bo'laklar testdan o'tdi).
- Ghost natijasi saqlanmaydi (spec: server hisoblamaydi).

KEYINGI BOSQICH: Bosqich 6 — 5x5 Jamoaviy (TeamRace, TeamRaceHub, jamoa WPM)
═══════════════════════════════════════════════════════════════
