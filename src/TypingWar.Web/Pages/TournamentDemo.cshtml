@page
@model TournamentDemoModel
@{
    ViewData["Title"] = "Turnir simulyatsiyasi";
    ViewData["Robots"] = "noindex, nofollow";
}

<div id="tw-demo" class="tw-demo">

    <!-- Sarlavha -->
    <header class="tw-demo-hero">
        <span class="tw-demo-badge"><i class="bi bi-cpu"></i> Simulyatsiya</span>
        <h1>Turnir simulyatsiyasi</h1>
        <p>O'yinchilar sonini o'zingiz tanlang (1–64) va playoff bracket /Tournaments sahifasida
           qanday ko'rinishini kuzating — yaratish, boshlash va har bir raundni qadam-baqadam o'ynash.
           Bracketni xarita kabi kattalashtirib (zoom) va sudrab ko'ring.</p>
    </header>

    <!-- Boshqaruv paneli -->
    <div class="tw-demo-panel">
        <!-- O'yinchilar sonini kiritish (1–64) -->
        <div class="tw-demo-countrow">
            <label for="tw-d-count"><i class="bi bi-people-fill"></i> O'yinchilar soni</label>
            <div class="tw-demo-count-field">
                <input id="tw-d-count" type="number" min="1" max="64" step="1" value="8"
                       class="tw-demo-count-input" inputmode="numeric" autocomplete="off" />
                <span class="tw-demo-count-hint">1–64 oralig'ida</span>
            </div>
        </div>

        <div class="tw-demo-controls">
            <button id="tw-d-create" class="tw-rbtn tw-rbtn--gold">
                <i class="bi bi-people-fill"></i><span>8 o'yinchili turnir yaratish</span>
            </button>
            <button id="tw-d-start" class="tw-rbtn tw-rbtn--outline" disabled>
                <i class="bi bi-flag-fill"></i><span>Turnirni boshlash</span>
            </button>
            <button id="tw-d-round" class="tw-rbtn tw-rbtn--outline" disabled>
                <i class="bi bi-play-fill"></i><span>Keyingi raundni o'ynash</span>
            </button>
            <button id="tw-d-auto" class="tw-rbtn tw-rbtn--outline" disabled>
                <i class="bi bi-fast-forward-fill"></i><span>Avtomatik (oxirigacha)</span>
            </button>
            <button id="tw-d-reset" class="tw-rbtn tw-rbtn--danger" disabled>
                <i class="bi bi-trash3"></i><span>O'chirish / yangidan</span>
            </button>
        </div>
        <div id="tw-d-status" class="tw-demo-status">O'yinchilar sonini kiriting (1–64) va «Turnir yaratish» tugmasini bosing.</div>
    </div>

    <div id="tw-d-err" class="tw-rcard-err text-center"></div>

    <!-- Chempion banneri -->
    <div id="tw-d-champion" class="tw-champion d-none"></div>

    <!-- Ikki tomonlama playoff bracket — MARKAZDA 3D oltin gepard kubogi (to'liq kenglik) -->
    <section id="tw-d-bracket-wrap" class="tw-bracket-wrap d-none">
        <div class="tw-bk-head">
            <h3 class="tw-bracket-title"><i class="bi bi-diagram-3"></i> Playoff bracket</h3>
            <div id="tw-d-bracket-tools" class="tw-bkz-tools">
                <button type="button" class="tw-bkz-btn" data-bkz="out" title="Kichraytirish"><i class="bi bi-dash-lg"></i></button>
                <button type="button" class="tw-bkz-btn" data-bkz="fit" title="Ekranga sig'dirish"><i class="bi bi-aspect-ratio"></i></button>
                <button type="button" class="tw-bkz-btn" data-bkz="in" title="Kattalashtirish"><i class="bi bi-plus-lg"></i></button>
                <button type="button" class="tw-bkz-btn" data-bkz="full" title="Butun ekran"><i class="bi bi-fullscreen"></i></button>
            </div>
        </div>
        <button type="button" class="tw-bkz-close" title="Chiqish (Esc)"><i class="bi bi-x-lg"></i></button>

        <div id="tw-d-bracket-viewport" class="tw-bk-viewport">
            <div id="tw-d-bracket-canvas" class="tw-bk2">
                <!-- Chap yarm -->
                <div id="tw-d-bracket-left" class="tw-bk2-side"></div>

                <!-- Markaz: 3D OLTIN GEPARD KUBOGI + final o'yini -->
                <div class="tw-bk2-center">
                    <div class="tw-trophy-stage">
                        <div id="tw-trophy-3d" class="tw-trophy-3d">
                            <div class="tw-trophy-fallback"><i class="bi bi-trophy-fill"></i></div>
                        </div>
                        <div id="tw-trophy-caption" class="tw-trophy-caption">
                            <i class="bi bi-hourglass-split"></i> Chempionni kutmoqda…
                        </div>
                    </div>
                    <div id="tw-d-bracket-final" class="tw-bk2-final"></div>
                </div>

                <!-- O'ng yarm (ko'zguda) -->
                <div id="tw-d-bracket-right" class="tw-bk2-side tw-bk2-side--right"></div>
            </div>
        </div>
        <div class="tw-bkz-hint">
            <i class="bi bi-mouse"></i> G'ildirak — zoom &nbsp;·&nbsp;
            <i class="bi bi-arrows-move"></i> sudrab surish &nbsp;·&nbsp;
            <i class="bi bi-arrows-fullscreen"></i> butun bracket
        </div>
    </section>

    <div class="tw-demo-grid">
        <!-- Chap: o'yinchilar / yakuniy natijalar -->
        <section class="tw-demo-main">
            <h2 class="tw-demo-h2"><i class="bi bi-person-badge"></i> Ishtirokchilar</h2>
            <div id="tw-d-seeds" class="tw-demo-seeds"></div>
            <div id="tw-d-standings" class="tw-demo-standings d-none"></div>
        </section>

        <!-- O'ng: jarayon jurnali -->
        <aside class="tw-demo-side">
            <h2 class="tw-demo-h2"><i class="bi bi-list-ol"></i> Jarayon jurnali</h2>
            <div id="tw-d-log" class="tw-demo-log">
                <div class="tw-demo-log-empty">Bu yerda har bir qadam yoziladi…</div>
            </div>
        </aside>
    </div>
</div>

@section Scripts {
    <script src="~/js/tw-bracket-layout.js" asp-append-version="true"></script>
    <script src="~/js/tw-bracket-zoom.js" asp-append-version="true"></script>
    <script src="~/js/tournament-demo.js" asp-append-version="true"></script>
    <script type="module" src="~/js/tournament-trophy.js" asp-append-version="true"></script>
}
